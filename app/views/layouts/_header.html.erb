<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "R-Gallery", root_path, id: "logo" %>

      <nav>
        <ul class="nav pull-right">
          <% if signed_in? %>
              <li><%= link_to "Главная", controller: "users", action: 'show', id: current_user.id %></li>
              <li><%= link_to "Пользователи", users_path %></li>
              <li><%= link_to "Загрузить", upload_path %> </li>

          <% else %>
              <li><%= link_to "Главная", root_path %></li>
          <% end %>

          <li><%= link_to "Помощь", help_path %></li>

          <% if signed_in? %>
              <li><%= link_to "Выйти", signout_path, method: "delete" %> </li>
          <% else %>
              <li><%= link_to "Войти", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
