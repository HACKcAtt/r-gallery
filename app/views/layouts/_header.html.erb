  <div class="header navbar navbar-fixed-top navbar-inverse ">
    <div class="container ">
      <div class="navbar-header">

      <%= link_to "R-Gallery", root_path, id: "logo", :class=>"navbar-brand" %>


      <button class = "navbar-toggle" data-toggle = "collapse" data-target = ".navHeaderCollapse">

        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      </div>
      <div class="collapse navbar-collapse navHeaderCollapse">
        <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
              <% if current_page?(:controller => 'users', :action => 'show', :id => current_user.id) %>
                  <li class="active">
              <% else %>
                  <li>
              <% end %>
                <%= link_to "Главная", controller: "users", action: 'show', id: current_user.id %></li>

                  <% if current_page?(users_path) %>
                      <li class="active">
                  <% else %>
                      <li>
                  <% end %><%= link_to "Пользователи", users_path %></li>

                      <% if current_page?(upload_path) %>
                          <li class="active">
                      <% else %>
                          <li>
                      <% end %><%= link_to "Загрузить", upload_path %> </li>

          <% else %>
              <% if current_page?(root_path) %>
                  <li class="active">
              <% else %>
                  <li>
              <% end %><%= link_to "Главная", root_path %></li>
          <% end %>

          <% if current_page?(help_path) %>
              <li class="active">
          <% else %>
              <li>
          <% end %><%= link_to "Помощь", help_path %></li>

          <% if signed_in? %>
              <li><%= link_to "Выйти", signout_path, method: "delete" %> </li>
          <% else %>
              <li><%= link_to "Войти", signin_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>